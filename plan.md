# EX Shogi Web - Project Plan

EX将棋のブラウザ版です。モバイル版の資産（エンジン、ロジック、PvP protocol、棋譜、観戦設計）を最大限に再利用しながら、WebならではのプレミアムなUI/UXを提供します。

このプロジェクトの最小実用条件は、全5バリアントでブラウザ対局が成立し、モバイル版と同じ PvP サーバ上で Web とスマホが同じルームに参加できることです。さらに、PvC 練習、鍵付きロビーによるフレンドマッチ、公開ロビーからの観戦を Web 版の中核体験に含めます。Web版では観戦リアクションは扱いません。見た目のプレミアムさは後回しの装飾ではなく、EX将棋らしさを伝える入口体験として維持します。

## 要求事項 (Requirements)
- 全5バリアント（とるいち / 影武者 / インベーダー / 罠 / 神隠し）で PvP 対局ができる。
- 全5バリアントで PvC 対局ができる。
- PvC はモバイル版の CPU レベル Lv1-5 方針と棋譜メタデータ方針に合わせる。
- ブラウザとスマホが同じ PvP サーバ、同じルーム、同じ protocol で対局できる。
- ルームキー/パスワードを設定し、同じキーを知る相手とフレンド対戦ができる。
- 公開ロビーから対局を選んで観戦できる。
- 観戦者は着手できず、状態同期、終局表示、最低限のリアクションのみ利用できる。
- モバイル版の共有エンジン / ルール / 棋譜パッケージを最大限再利用する。
- デスクトップブラウザに最適化された、リッチでスムーズなUI。
- プレミアムな質感（アニメーション、グラスモーフィズム等）。

## MVP Scope
- Home からバリアント選択、PvC、ロビー一覧、ルーム作成、ルームキー入力、PvP対局、観戦、結果表示まで到達できる。
- 全5バリアントのルール差分が対局に反映される。
- PvC は練習導線として扱い、オンライン待機中の補助体験にも接続できるようにする。
- ルームキーはフレンド対戦用の合流キーとして扱う。公開ロビーは観戦/参加導線として扱い、自動マッチングやレーティングは含めない。
- 対局中の基本操作（選択、移動、打ち、成り、投了または終了処理）が成立する。
- 棋譜/リプレイは最小限の表示または将来接続前提でよいが、対局状態と観戦状態が壊れないことを優先する。

## Out of Scope
- 自動マッチング、ランキング、レーティング。
- モバイル版と同等の全機能。
- 本格的なアカウント基盤、フレンドリスト、通知。
- 高度な観戦分析、棋譜検索、UGCタグ運用。

## 制約 (Constraints)
- `react-native-web` は使用せず、React DOM で構築する。
- linked package として参照している `mobile/exshogi` 側の engine packages を壊さない。
- Web版固有の実装は、モバイル版の PvP / 保存 / エンジン方針と矛盾させない。
- サーバ protocol、RoomPreset、RoomSnapshot、棋譜/リプレイの意味はモバイル版と互換にする。
- 本将棋/Standard を正式モードとして追加しない。現在の Standard 固定実装は移行用の暫定状態として扱う。
- WASM は必要に応じて使うが、まずは既存共有パッケージで最低限の対局成立を優先する。
- 外部入力（チャット等）がある場合は Model Armor で保護する。

## ゴール (Goals)
- [ ] 全5バリアントでPvP対局が成立する。
- [ ] 全5バリアントでPvC対局が成立する。
- [ ] Web とスマホが同じサーバ/同じルームで対局できる。
- [ ] ルームキーによるフレンド対戦ができる。
- [ ] 公開ロビーから観戦できる。
- [ ] スムーズな駒操作と高品質な視覚効果。
- [ ] プレミアムな入口体験として見た目の質感を維持する。

## Done Conditions
- 2つのブラウザセッションで同じルームキーを使い、同一ルームへ参加できる。
- Web とスマホで同一ルームへ参加し、交互に指せる。
- 公開ロビーから観戦者として接続し、観戦者が着手できないことを確認できる。
- 全5バリアントで PvP / PvC を最低1局ずつ対局開始から終了まで確認できる。
- 主要な禁則、成り、打ち、バリアント固有処理が共有エンジン方針と矛盾しない。
- `pnpm build` と `pnpm lint` が通る。
- 未実装のモバイル版機能や既知制約が `task.md` に残っている。
